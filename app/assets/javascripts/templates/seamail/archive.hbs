<div>
  <span class="pull-right">
    {{#link-to "seamail.inbox" class="btn btn-info btn-xs"}}Inbox{{/link-to}}
    {{#link-to "seamail.outbox" class="btn btn-info btn-xs"}}Outbox{{/link-to}}
  </span>

  <h3>Archive</h3>
  {{#each mail in controller.seamail}}
    <div class='seamail-entry container'>
      {{ render "seamail/details" mail }}
    </div>
  {{else}}
    {{#unless loading}}
      <div>
        Nothing archived
      </div>
    {{/unless}}
  {{/each}}
  {{partial 'loading'}}
</div>
{{partial 'bottomMenu'}}
